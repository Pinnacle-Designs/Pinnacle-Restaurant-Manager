# Private materials (not deployed)

Files here are **not** served on the public marketing site (`docs/`) or at `/docs/`.

## Full pitch deck

Open `pitch-deck.html` locally in a browser and use **Print → Save as PDF** when you need to share with investors, licensees, or acquirers.

Do not link this file from the public site. Partnership inquiries go through **docs/pitch.html** → private deck sent manually after review.

## Sales deck

Open `sales-deck.html` locally and use **Print → Save as PDF**, or share the unlisted app URL:

`/sales-deck?key=<SALES_DECK_ACCESS_KEY>`

Set `SALES_DECK_ACCESS_KEY` in production env. The route is not linked from the marketing site or navigation.
