/** Shown only when printing — timestamp set by printReport(). */
export function PrintReportStamp() {
  return (
    <p
      id="print-report-stamp"
      className="print-only text-xs text-slate-500"
      aria-hidden
    />
  );
}
